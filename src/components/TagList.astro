---
import { slugify } from "../utils/slugify";

const { tags = [] } = Astro.props;
---

{tags.length > 0 ? (
  <ul class="tag-list">
    {tags.map((tag) => (
      <li>
        <a class="tag" href={`/tags/${slugify(tag)}/`}>{tag}</a>
      </li>
    ))}
  </ul>
) : null}
